# Проверка сборки 0.5.0 · 22.09.2026

- Базовый исходный репозиторий: `imkonex-cars/imkonex-wheels`, commit `9a03876d0cb39a8c7de5c88961bf41735eed43cb`.
- Публичная выгрузка: 20 359 товаров (7 055 шин и 13 304 диска), 22.09.2026 04:09 UTC. Данные и фотографии сохранены.
- `npm test`: 25 проверок пройдено. Проверены фильтры, каскад автомобиля, обработка ошибки API, категории, корзина, остатки одного склада, подтверждение заказа и отсутствие закупки в клиентском запросе. На синтетическом каталоге из 24 000 товаров отрисовываются 12 карточек; страницы и точный поиск работают.
- `python -m pytest -q`: 122 проверки и 15 подслучаев пройдено. Проверены прежние адаптеры, новые категории/камеры, схемы подбора, авторизация, CSRF, цены по складам, сохранение после перезапуска, отзыв сессий, отсутствие закупки в публичном ответе, предпросмотр, изменение цены/остатка, запрет дублей и неоднозначный ответ поставщика.
- Запуск настоящего локального Uvicorn-процесса с текущей полной выгрузкой: `/health`, динамический `config.js`, 20 359 товаров с gzip, `/manager/`, вход и закрытые товары отвечают правильно. Измеренный RSS в этом ограниченном сценарии около 158 МиБ; это не нагрузочный тест.
- Сборка `node BUILD_RENDER.mjs` создаёт `dist` с версией 0.5.0 и полным публичным каталогом.
- Тесты выполнялись с синтетическими секретами и ответами. Во время проверки не выполнялось ни одного запроса к API с данными вашей учётной записи и не создавались реальные/тестовые заказы поставщику.
- Два предупреждения тестовой среды относятся к устаревающему интерфейсу Starlette TestClient/httpx; ошибок выполнения нет.

Границы проверки: Docker-контейнер не запускался (Docker отсутствует в среде). Полная браузерная визуальная проверка локального сайта заблокирована политикой браузерной среды. Реальные ответы новых методов и внешний вид на Render проверяются после установки. Сайт 74kolesa.ru не удалось изучить из-за отказа доступа; дизайн является самостоятельным.
