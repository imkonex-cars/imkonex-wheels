{
  "name": "imkonex-wheels",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node BUILD_RENDER.mjs",
    "test": "node --test tests/frontend.test.mjs tests/snapshot.test.mjs tests/workspace.test.mjs",
    "check": "node --check frontend/app.js && node --check frontend/domain.js",
    "start": "python3 -m http.server 4173 --directory dist"
  },
  "devDependencies": {
    "jsdom": "^27.4.0"
  }
}
